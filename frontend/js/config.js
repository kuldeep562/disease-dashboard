/* config.js — Disease Dashboard */
const CFG = {
  API_BASE: window.__API_BASE__ || `${window.location.protocol}//${window.location.host}/api`,
  APP_NAME: 'MedWatch',
  VERSION:  '2.1.0',
  PAGE_SIZE: 20,
};
